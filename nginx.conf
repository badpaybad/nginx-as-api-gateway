upstream backend_test {
     	# server 172.16.16.253:9998;
		# server mldlai2vcpu2gbram.sunteco.cloud:80;
		# server mldlai2vcpu2gbram.sunteco.cloud:443;
		# server mldlai2vcpu2gbram.sunteco.cloud;
		#server 118.70.117.208:9998;
		server 172.16.16.253:8886;
		# server airobotic-api-v1.k8s-dev.omt.vn:443;
		# server esadmin.k8s-dev.omt.vn
}

upstream backend_others {
		#server esadmin.k8s-dev.omt.vn:443;
		server 172.16.16.253:8887;
}

upstream backend_default {
		#server esadmin.k8s-dev.omt.vn:443;
		server 172.16.16.253:8888;
}

server { 
	listen 80;
	server_name _;

	location ~* /TestRouting1/ {		
		proxy_set_header        Host $host;
		proxy_set_header        X-Real-IP $remote_addr;
		proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header        X-Forwarded-Proto $scheme;
		proxy_set_header 		X-Original-URI $request_uri;
		
		proxy_pass http://backend_test$request_uri;		

	}
	
	location ~* /Others/ {		
		proxy_set_header        Host $host;
		proxy_set_header        X-Real-IP $remote_addr;
		proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header        X-Forwarded-Proto $scheme;
		proxy_set_header 		X-Original-URI $request_uri;
		
		proxy_pass http://backend_others$request_uri;		

	}


	location / {
		#mirror /mirror;
     	#mirror_request_body on;

		# root /usr/share/nginx/html;
		# try_files $uri /swagger/index.html;
        # index  index.html index.htm;
		proxy_set_header        Host $host;
		proxy_set_header        X-Real-IP $remote_addr;
		proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header        X-Forwarded-Proto $scheme;
		proxy_set_header 		X-Original-URI $request_uri;

		proxy_pass http://backend_default$request_uri;		

	}



    error_page   500 502 503 504 404  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}